#include "exponential_func.h"

namespace MATH {
void ExpoFunc::FuncParser(const std::string& func) {
  std::regex pattern(
      R"(^([-+]?)(?:(?:(\d*\.?\d+)|([-+]?))?(?:(x(?:\^(-?\d+))?)?(?:(y(?:\^(-?\d+))?)?(?:(z(?:\^(-?\d+))?)?$)");
    // TODO: 待总函数字符串解析好之后再处理
}
}  // namespace MATH